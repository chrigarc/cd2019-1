Entrar a la página https://github.com/chrigarc/cd2019-1 y dar clic al botón de fork.

Clonar el repositorio al que se ha hecho fork: git clone git@github.com:jmct1208/cd2019-1.git

Creamos una rama de ejemplo: git checkout -b produccion

Agregamos el repositorio del que hicimos fork como upstream:
git remote add upstream https://github.com/chrigarc/cd2019-1

Hacemos pull de upsteam: git pull upstream master

Creamos una carpeta cuyo nombre es 415030584 y ponemos este archivo de texto.

Agregamos los cambio y hacemos commit.
git add . y git commit -m "mensaje relevante"

Luego hacemos push al repositorio remoto.
git push origin produccion

Solicitamos pull request.



